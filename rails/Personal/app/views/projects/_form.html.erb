<%= simple_form_for([@github_address, @post.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :commenter %><br />
    <%= f.text_field :commenter %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>